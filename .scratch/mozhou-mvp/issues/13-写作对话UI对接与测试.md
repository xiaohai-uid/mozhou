# 13 — 写作对话 UI 对接与测试（MVP 1.0 主线）

**What to build:** 将【写作对话】主界面前端组件与已实现的 `POST /api/v1/chat` SSE 流式 API 完成对接验证：输入提示词 → 接收 SSE 流式文本 → 界面逐字渲染。这是 MVP 1.0 的救活闭环（全局规范 v1.0.0 第四节 Step 3/4）。

**Blocked by:** 04 流式写作对话（API 已实现，a371331）

**Status:** ready-for-agent

**范围锁死（规范第二节）**：只允许触碰【写作对话】相关代码；其余 11 界面只读冷冻。改动文件 ≤ 3 个。

- [x] 契约测试回归：`vitest run`（http 项目：auth + chat，SSE 全链路 mock provider）— 40/40 全绿
- [x] 浏览器真实 E2E：登录 → 新会话 → 发送提示词 → 逐字渲染验证（真实模型 one-api 双渠道）— DeepSeek + GLM 双模型均通过
- [x] 若 UI 与 API 契约不匹配（事件类型/字段），修复对应函数（≤3 文件）— 无需修复，零代码改动
- [x] 通过后 git commit（独立提交，与 UI 归档隔离）— efb9a71 之后独立提交

**验证记录（2026-08-09）**：契约测试 40/40（unit 21 + http 19，chat SSE 全链路 8 项）；浏览器 E2E：DeepSeek 流式渲染 ✅ / 刷新持久化 ✅ / GLM 新会话流式 ✅（进行中态占位+光标+禁用、完成后完整渲染）。

**契约对照**：`.scratch/mozhou-mvp/contracts/api-contract.md` 第 2 节（会话 CRUD + SSE 事件流：start/delta/error）
