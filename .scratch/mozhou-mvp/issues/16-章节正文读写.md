# 16 — 章节正文读写（Walking Skeleton 第一切片）

**What to build:** 章节正文落库（chapters.content）+ 读写端点 + 章节页正文区接真实（自动保存防抖 + 保存状态真实化）。

**Blocked by:** —（契约 21 已 Frozen：Interaction/API/Domain/Persistence）

**Status:** done（2026-08-11）

**契约（21-章节级续写契约.md 第 2 节）：**
- `ALTER TABLE chapters ADD COLUMN content text NOT NULL DEFAULT ''`（migration 0011，05.5 流程应用）
- `GET /api/v1/novels/{novelId}/chapters/{chapterId}` → ChapterDetail（含 content）
- `PATCH /api/v1/novels/{novelId}/chapters/{chapterId}` body `{ content? }`（保存正文；自动保存与显式保存同端点）
- 归属校验（他人章节 404）；title/status 既有 PATCH 字段保持

- [ ] migration 0011（chapters.content）+ 05.5 流程应用
- [ ] 服务层：getChapterDetail / updateChapterContent（复用 lib/novels/service.ts）
- [ ] 路由：GET/PATCH 章节详情（含 content），归属校验
- [ ] 契约测试：读写往返 / 越权 404 / PATCH 空 body 400 / 未登录 401
- [ ] 章节页正文区接真实：加载 content + 2s 防抖自动保存 + 显式保存 + 保存状态（未保存/保存中/已保存/保存失败）——替换 mock 本地态
- [ ] 契约文档 api-contract.md 补第 21 节引用

**验收：** 打开章节看到真实正文；编辑 → 未保存 → 防抖保存 → 已保存；刷新页面正文保留。
