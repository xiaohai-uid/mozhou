# 17 — 章节对话引擎（对话留存 + 技能驱动生成）

**What to build:** 章节对话消息持久化（chapter_messages）+ GET 历史 + POST chat SSE（正文上下文 + 技能/风格/RAG 注入 + AI 消息落库 + 停止→stopped + 错误码）→ 对话面板接真实（消息加载/发送/流式/停止/技能胶囊真实库）。

**Blocked by:** 16 章节正文读写（正文是注入上下文）

**Status:** done（2026-08-11）

**契约（21-章节级续写契约.md 第 2/3/4 节）：**
- migration 0012：`chapter_messages` 表（role/content/skills jsonb/snapshot/status/inserted/created_at + chapter_id 索引）
- `GET /api/v1/novels/{novelId}/chapters/{chapterId}/messages` → 消息历史（新→旧）
- `POST /api/v1/novels/{novelId}/chapters/{chapterId}/chat`（SSE：start→delta*→done{messageId}|error{code}）
- system 注入顺序：正文参考（末尾 3000 字）→ RAG 设定（novelId，复用 retrieveContext）→ 风格（styleId，复用 15）→ 技能（skills）
- AI 消息落库：skills 快照 + snapshot=生成时正文
- 客户端断开 SSE → AI 消息标记 stopped（保留已生成部分）
- 错误码事件：AiNoApiKey/AiRateLimited/AiServerError/AiTimeout/AiNetworkError/AiInvalidResponse

- [ ] migration 0012（chapter_messages）+ 05.5 流程应用
- [ ] 服务层：listChapterMessages / runChapterChat（复用 runNodeStream + makeChatProvider；注入链组装）
- [ ] 路由：GET messages + POST chat（SSE + 断开停止）
- [ ] 契约测试：消息历史往返 / SSE 注入断言（mock 回显含 [正文参考]/[技能]/[风格]）/ 消息落库（skills+snapshot）/ 断开→stopped / 错误码事件 / 越权 404 / 未登录 401
- [ ] 对话面板接真实：历史加载（对话留存跨会话）+ 发送/流式/停止（断开 fetch）+ 技能胶囊换真实库（场景技能内置常量 + /api/v1/skills?scope=mine）+ 注入标签用消息 skills 快照
- [ ] 空章节起笔按钮接真实（触发「章节起笔」技能发送）

**验收：** 章节页对话真实往返（mock provider 回显可见注入链）；刷新页面对话留存；技能胶囊=内置场景技能+我的技能。
