# 15 — 对话应用风格库（V1.1 Journey ⑥ 后半）

**What to build:** chat 风格胶囊数据源改为真实风格库（演示三件套删除）；请求改 `styleId` 引用；服务端查 styles 表注入完整四维指南到 system 提示；蒸馏页"应用到对话"回流升级为 `{styleId, name}`。

**Blocked by:** 14 风格库保存与管理

**Status:** done（2026-08-11，提交 82c72c7）

**验收：** 胶囊数据源换真实库（演示三件套删除）+ styleId 注入测试 3/3（四维格式断言/他人归属不注入/不存在静默忽略，mock 回显 system 提示）+ gate:ticket 通过 + 浏览器 E2E（胶囊真实库、发送后 mock 回显可见 `[风格] …四维指南`、蒸馏页回流 styleId 校验自动选中 DOM 断言）

- [x] chat-view：加载 `/api/v1/styles` 作胶囊数据源；style 状态改 `{id, name} | null`
- [x] chat-view：pending 回流读取升级（styleId 校验）；请求体 `style` → `styleId`
- [x] chat 路由：body 解析 `style` → `styleId`
- [x] chat service：`RunChatInput.style` → `styleId`；查表注入四维指南（写路径归属校验）
- [x] 契约文档 api-contract.md：chat 请求体更新 + 注入格式说明
- [x] 契约测试：styleId 注入（mock 断言 system 含四维指南）、无效 styleId 静默忽略、缺失 404 会话回归

**已冻结决策（spec.md V1.1 章节 + CONTEXT.md 术语表）：**
- 胶囊 = 无 + 我的风格库；演示风格三件套（灰烬写实/意象绵长）彻底删除
- chat 契约：`style` 自由文本删除，改 `styleId`（可空）
- 服务端注入格式：`[风格] 名称：叙事视角——…；句式节奏——…；意象偏好——…；情绪节奏——…`
- pending 回流：读 `{styleId, name}`，校验 styleId 在已加载风格库中才选中

- [ ] chat-view：加载 `/api/v1/styles` 作胶囊数据源；style 状态改 `{id, name} | null`
- [ ] chat-view：pending 回流读取升级（styleId 校验）；请求体 `style` → `styleId`
- [ ] chat 路由：body 解析 `style` → `styleId`
- [ ] chat service：`RunChatInput.style` → `styleId`；查表注入四维指南（写路径归属校验）
- [ ] 契约文档 api-contract.md：chat 请求体更新 + 注入格式说明
- [ ] 契约测试：styleId 注入（mock 断言 system 含四维指南）、无效 styleId 静默忽略、缺失 404 会话回归
