# 01 — 墨舟脚手架

**What to build:** 开发者（你）在本机一条命令拉起整个墨舟开发环境：Next.js 应用、PostgreSQL（含 pgvector 扩展）、one-api 网关三件套通过 Docker Compose 编排启动，浏览器打开能看到墨舟品牌首页（深色编辑器风）。这是所有后续票的地基。

**Blocked by:** None — can start immediately

**Status:** ✅ done (2026-08-09)

- [x] Docker Compose 编排三个服务（app[profile prod] / postgres+pgvector / one-api）并能一键启动
- [x] Next.js (App Router) + TypeScript + Tailwind + shadcn/ui 项目初始化完成
- [x] Drizzle ORM 接入 Postgres，迁移机制可运行（generate/migrate + users 表 + tier 枚举 + pgvector 扩展）
- [x] 品牌首页（墨舟、深色主题）可访问（curl 验证 3002 端口）
- [x] one-api 可访问并配置了 DeepSeek + Qwen 渠道（scripts/init-one-api.sh：登录→渠道×2→应用令牌，已执行验证）
- [x] 项目内记录启动命令（README）

**实施备注**（code-review 双轴发现均已修复）：
- db.ts/drizzle.config.ts 删除重复的 URL 回退常量（消除 5432/5433 脱节隐患）
- tier 用 pgEnum（free/member）替代裸 text
- Docker 镜像源受限 → app 服务挂 profile: ["prod"]（开发用宿主 npm run dev）
- 本机 WSL Postgres 占用 5432 → 映射 5433
