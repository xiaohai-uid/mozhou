# 14 — 风格库保存与管理（V1.1 Journey ⑥ 前半）

**What to build:** 蒸馏出的四维风格指南可命名保存到"我的风格库"（styles 表），列表展示、可删除；保存后可"应用到对话"回流。

**Blocked by:** —（V1.0 已归档；spec V1.1 章节已冻结）

**Status:** ready-for-agent

**已冻结决策（spec.md V1.1 章节 + CONTEXT.md 术语表）：**
- 管理入口 = 蒸馏页内嵌区块（保存/列表/删除同页闭环）
- 同名允许；不设上限/分页（全量返回）；限次不扩展
- 编辑/重命名不入本工单
- "应用到对话"按钮写 `{styleId, name}` 到 sessionStorage（name 保留兼容 chat 旧读取器，工单 15 读 styleId）

- [ ] `styles` 表（id/user_id/name/guide_json(jsonb 四维)/created_at，skills 表为模板）→ migration 0010 手动 psql 应用 + 写入 `drizzle.__drizzle_migrations`（05.5 流程）
- [ ] `GET /api/v1/styles` — 我的风格库列表（desc createdAt）
- [ ] `POST /api/v1/styles` — 保存（name + guide 四维校验）
- [ ] `DELETE /api/v1/styles?id=` — 删除（写路径归属校验防 IDOR，参照 chat assertOwned 注释风格）
- [ ] 蒸馏页 UI：生成指南后内联命名（默认名"风格_MMDD"）→ 保存按钮 → 成功提示 + "应用到对话"按钮
- [ ] 蒸馏页 UI："我的风格库"列表区块（名称/日期/删除按钮，保存后即时刷新）
- [ ] 契约文档 api-contract.md 新增 styles 节
- [ ] 契约测试：未登录 401 / 保存校验 400 / CRUD 全链路（含归属删除 404）
