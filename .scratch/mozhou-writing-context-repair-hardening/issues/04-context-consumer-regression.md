## What to build

将独立/章节 consumer 接入统一语义与 wire seam，验证压缩、RAG ownership、章节历史、SSE 和真实 one-api smoke。

**Blocked by:** 01-final-wire-payload, 02-production-provider-guard, 03-observer-derived-facts  
**Status:** ready-for-agent

## Acceptance

TypeScript、完整测试、production build、数据库/HTTP consumer、真实模型 smoke 通过；不包含候选生命周期。
