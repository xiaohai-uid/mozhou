# 交接文档 · 墨舟 MoZhou Novel OS（2026-09-17）

> 用途：把本仓库的全貌、进度、规矩与下一步完整交给下一位 AI。
> 开工前必读顺序：本文件 → `AGENTS.md`（工程规矩 UVSD v2.2.1）→ `docs/superpowers/plans/2026-09-15-full-release/START-HERE.md`（当前执行计划入口）→ `tasks.json`。

---

## 0. 一句话定位

**墨舟（MoZhou / Novel OS）= 长篇小说 AI 辅助创作操作系统**：本地优先（local-first）的 Web + 桌面应用，核心卖点是工业级状态机、11 项机械质量门禁、确定性长程因果契约、4-gram 去复读、USER_BYOK 真实模型流式生成。当前版本定位 **Technical Preview / 社区免费版（商业化通道未开放）**。

- 仓库根：`C:\zcode\novel-ai`
- 远端：`https://github.com/xiaohai-uid/mozhou.git`（origin）
- GitNexus 图谱：repo 名 `mozhou`（**查询时传绝对路径 `C:\zcode\novel-ai`**，因为有 3 个同名 worktree 兄弟仓库），规模 8947 节点 / 19713 边 / 532 执行流 / **0 循环依赖 clean**

---

## 1. 技术栈与目录结构

```
novel-ai/                          ← pnpm monorepo（Node>=22, pnpm@9.15.0, TypeScript）
├── apps/web/                      ← 主应用：Next.js（工作台 UI + server/ BFF 路由层）
│   ├── src/                       workbench 编辑器/分镜/书架/任务中心/技能广场等（UI 基线 = Ink Orbit, ADR-0027）
│   └── server/                    API 路由：proseRoutes / draftCandidates / billing/catalog / storyboard 等
├── packages/                      ← 8 个 @mozhou/* 核心包（全部有 src，非壳）
│   ├── kernel/                    Story Brain 认知穿透、内核 schema（Author Intent / Outline Graph / Scene / Temporal Fact / Canon Fact）
│   ├── pipeline/                  本地十步生产管道（47 文件，最大包）：draft→review→quality-rework→commit 状态机
│   ├── quality-engine/            11 项机械门禁 + 4-gram 去复读 + SEPIA 审查规则
│   ├── context-compiler/          确定性三通道加权 RRF 上下文装配
│   ├── data-plane/                本地 SQLite WAL 数据平面（36 文件，drizzle）
│   ├── flywheel/                  数据飞轮
│   ├── runtime/                   运行时
│   └── benchmark/                 质量回归基准（quality-regression.test.ts）
├── src-tauri/                     ← Tauri 桌面壳（productName "MoZhou Novel OS"，v0.1.0）
├── app/                           ← 遗留 Next.js 应用（早期版本，勿与 apps/web 混淆；大部分功能已迁走）
├── archive-legacy/                ← 归档旧代码
├── prototype/, prototypes/        ← UI 原型留档
├── docs/
│   ├── adr/                       决策记录
│   ├── superpowers/plans/         ← ★ 历次执行计划（最新：2026-09-15-full-release/）
│   └── audit/                     审查包（如 review-handoff-2026-08-18.md）
├── evidence/                      ← ★ 交付证据（full-release/<run-id>/T00-T05/ 的 result.json + 日志）
├── specs/                         001-004 商业化/编辑器/视觉图谱/杰作票
├── scripts/                       launcher / build-release / check-install-env
└── docker-compose.yml, Dockerfile, 启动墨舟.bat / start.sh
```

**测试规模**：499 个测试文件；最近基线 workspace 91 文件/740 tests + web 320 tests + 1 skip（真实模型测试无凭据按跳过）。README 宣称 888 项全绿为旧口径，以 evidence 里 T04/T05 记录为准（740+320）。

---

## 2. Git 状态（截至 2026-09-17）

| 项 | 值 |
|---|---|
| 当前分支 | `codex/mozhou-full-release-20260915` |
| HEAD | `62b3aa1`（docs(graph): 图谱块数字随 analyze 刷新 8947 nodes） |
| 工作树 | **干净（0 未提交）** |
| 是否已推送 | **否——该分支远端不存在**；比基线 `fed29e32` 领先 8 个提交 |
| 远端分支 | main / master / delivery-closure-20260905 / release-hardening-2026-09-03 / cloud-* / prototype-* 共 8 个 |
| push 纪律 | 每次推送必须走全局 `git-push-gate.cmd` 门禁（本地 act CI）；不支持 tag 直推 |

远端 master（`ef9f5b5`）与 T00 审计基线（`fed29e32`）不同——master 在 GitHub 上继续有过提交；交接时不要想当然把当前分支往 master 快进。

---

## 3. 当前进度：全功能收尾与双端发布计划（2026-09-15 run-1）

计划目录：`docs/superpowers/plans/2026-09-15-full-release/`（START-HERE.md / SPEC.md / CONTRACTS.md / tasks.json / EXTERNAL-INPUTS.json / AUDIT.md / regressions/）。目标：**公网可实际创作的网站 + Windows 本地应用，同一正式版本发布**；19/39 元两档会员；微信/支付宝真实支付；明确不做跨设备同步。

### 已完成（verified，证据在 evidence/full-release/20260915-1/）

- **T00** 保护现状与执行分支：22 项工作树修复审阅归为 baseline（R1/R2 保存契约 + 分镜适配），备份回读 mismatch=0，双套测试全绿。
- **T01** 外部资源探测：13 项外部输入 **12 项实查缺失**（登记 blocked，未编造）；Windows 工具链部分 ready（Rust 1.96 / WebView2 / signtool 有，签名证书无）。
- **T02** 定价目录冻结：1900/3900 分 CNY 唯一真源（Object.freeze）+ 不可售保护 + entitlementKeys 服务端裁决 + maxIncludedCalls 公式；21 新测试全绿。
- **T03** 候选契约 + 失败测试：draft-candidate.ts（C2 契约：候选零写入、跨进程持久、终态拒绝）；verify-stream red 日志留档。
- **T04** 流式候选化与受控采纳：verify-stream **红转绿**（originalLost=false）；draft.candidate/cancel/accept HTTP 端点；AbortSignal 贯穿；7 个并发/冲突/崩溃重放测试。
- **T05** UI 采纳链：DialogueStream 真实 /api/draft.accept + Undo + 409 冲突面板 + 迟到帧隔离。

### blocked（T06-T21 共 16 项，全部是"代码未实施 + 外部输入缺失"的如实登记）

阻塞根因链（细节在 tasks.json 各任务 blockedReason）：

1. **模型凭据**（无 MOZHOU/DEEPSEEK/OPENAI key）→ 堵 T07 → T06 十步生产 / T11 检索榜单 / T12 拆书文风 / T18 真实成本验收；
2. **身份与邮件**（无 Supabase 项目/回调/真实邮件）→ 堵 T08 → T09 租户隔离 → 几乎全部后续；
3. **支付商户**（无微信商户号/Native 权限、无支付宝应用签约、无测试预算授权）→ 堵 T14 → T15 权益额度；
4. **公网条件**（无主机/SSH/域名/证书/备份目的地）→ 堵 T16 → T19-T21；
5. **签名证书缺失** → 堵 T17 Windows 安装包闭环；
6. T19 独立安全验收按纪律**不得由执行 AI 自己签署**，须另一位审阅者或用户核对。

### 恢复执行的路径

按 START-HERE §3：`node docs/superpowers/plans/2026-09-15-full-release/check-handoff.mjs` 自检（exit 0 只代表交接结构 OK）→ 从 tasks.json 里第一个非 blocked 任务推进。用户若能提供任一外部输入（哪怕只有一个模型 key），对应链路即解锁。

---

## 4. 硬规矩（下一位 AI 必须遵守）

1. **UVSD Protocol v2.2.1**（AGENTS.md 全文）关键条款：一票一行为；禁止跨票重构；契约默认冻结（改契约须 Contract Delta）；生产禁止静默降级 mock；**绝不删/弱化/跳过失败测试来换取绿灯**；无人值守模式禁止 reset --hard/clean 未提交工作。
2. **证据纪律**：完成声明必须带 evidence JSON（kind/path/sourceCommit/command/exitCode/scope/limitations）；"132 个测试文件全绿"不能代替发布验收；外部输入缺失只能标 blocked，**禁止伪造通过**。
3. **GitNexus 门禁**：改前 `impact`（传 repo=`C:\zcode\novel-ai`）；交付前 `gitnexus check` 须 0 cycles；改后刷图谱 `node .gitnexus/run.cjs analyze`。
4. **发布纪律**：T20 之前不建公开 tag（防旧 release.yml 误触发）；执行者不得自签安全审查。
5. **同仓库的三个图谱兄弟**：`C:\Users\a1691\Documents\antigravity\happy-newton`（delivery-closure 线）和 `C:\zcode\novel-ai-closure` 也是 mozhou 的 worktree/克隆，图谱查询务必用绝对路径消歧。

---

## 5. 历史包袱与已知坑（避免重蹈）

- **旧 app/ 目录**是遗留 Next.js 应用（有 package-lock.json、drizzle、ONEAPI 遗留键名），主战场已是 apps/web + packages；不要把 app/ 当主应用改。
- **v0.1.0 发布包曾被判误导性**（README 宣称与真实能力有出入），当前定位降为 Technical Preview/社区免费版；云同步=本地快照模式，会员充值未开放——续作时口径必须延续这个"诚实定位"。
- **真实模型端到端从未验收**：所有真实 provider 旅程（T07 起）都没跑过，现有全部绿测试均为 mock/本地；交接时不要把"测试全绿"说成"生成功能已验证"。
- **web 端 1 个 skip 测试**（real-provider）是刻意保留的诚实跳过，不是遗漏。
- lint 曾有 1957 错的历史（release-hardening 线修复过），当前 eslint 配置含 allowDefaultProject 登记表（billing 测试目录在列），改配置须同步登记。
- 三份关键记忆线索（全局记忆）：T00-T05 交付于 2026-09-15；基线审计报告在 `C:\codex\audits\mozhou-release-20260914-fed29e3`；handoff 文档 `mozhou-handoff-2026-08-31.md` 在系统临时目录（可能已被清理，以本文件为准）。

---

## 6. 速查：常用验证命令

```bash
cd /c/zcode/novel-ai
pnpm install                     # 首次（有 preinstall 环境检查）
pnpm build                       # tsc -b 全量构建
pnpm test                        # workspace 全套
pnpm --filter @mozhou/web dev    # 启动 Web 工作台（localhost:5173）
node .gitnexus/run.cjs analyze   # 刷新图谱
# gitnexus check（MCP）→ 须 status: clean, cycleCount: 0
node docs/superpowers/plans/2026-09-15-full-release/check-handoff.mjs   # 计划交接自检
```

BYOK 真实生成通道（有 key 时）：`export DEEPSEEK_API_KEY=sk-...`（或 MOZHOU_API_KEY + MOZHOU_API_BASE + MOZHOU_MODEL）。

---

*交接完。下一位 AI 第一步：读 `AGENTS.md`，跑 check-handoff.mjs，对照 tasks.json 确认 T00-T05 仍为 verified、工作树仍干净，再决定从哪条 blocked 链推进（最大杠杆 = 用户提供模型 key 与 Supabase 项目）。*
